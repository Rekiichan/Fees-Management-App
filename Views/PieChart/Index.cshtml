@{
    Layout = "_Layout";
}

<div id="chartContainer" style="height: 600px; width: 100%;"></div>
<script type="text/javascript">
    window.onload = function () {

        var chart = new CanvasJS.Chart("chartContainer", {
            theme: "light1", // "light1", "light2", "dark1", "dark2"
            exportEnabled: true,
            animationEnabled: true,
            title: {
                text: "Parking fee statistics"
            },
            data: [{
                type: "pie",
                startAngle: 180,
                indexLabelFontFamily: "Garamond",
                indexLabelFontSize: 20,
                toolTipContent: "<b>{Pending Status}</b>: {value}%",
                indexLabel: "{label}: {y}%",
                dataPoints: @Html.Raw(ViewBag.DataPointsPendingStatus)
                            }]
        });
        chart.render();
    }
</script>

<script type="text/javascript">
    window.onload = function () {

        var chart = new CanvasJS.Chart("chartContainer", {
            theme: "light1", // "light1", "light2", "dark1", "dark2"
            exportEnabled: true,
            animationEnabled: true,
            title: {
                text: "Type car"
            },
            data: [{
                type: "pie",
                startAngle: 180,
                indexLabelFontFamily: "Garamond",
                indexLabelFontSize: 20,
                toolTipContent: "<b>{Type car}</b>: {value}%",
                indexLabel: "{label}: {y}%",
                dataPoints: @Html.Raw(ViewBag.DataPointsTypeCar)
                    }]
        });
        chart.render();
    }
</script>
